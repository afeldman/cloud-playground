playground:
  name: birdy-playground
  data_dir: ./data

aws:
  region: us-east-1

localstack:
  enabled: true
  endpoint: http://localhost:4566
  port: 4566

kind:
  enabled: true
  cluster_name: birdy-local
  kubeconfig: ~/.kube/config
